#include <stdio.h>
int main()
{
    // Variable Declaration
    char character;

    // Input Character
    printf("Enter a Character : ");
    scanf("%c", &character);

    // Display ASCII value
    printf("The ASCII Value is : %d", character);

    return 0;
}